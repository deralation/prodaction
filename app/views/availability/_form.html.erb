<%= form_for([equip, Availability.new]) do |f|%>
  <%= f.date_field :start_date %>
  <%= f.date_field :end_date %>
  <%= f.hidden_field :equipment_id, value: equip.id %>
  <br>
  <%= f.submit "Add availability"%>
<% end %>

